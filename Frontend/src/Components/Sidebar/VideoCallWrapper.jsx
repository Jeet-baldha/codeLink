import React from 'react';
import NVideoCall from './NVedioCall';

const VideoCallWrapper = ({ width, setWidth }) => {
    return <NVideoCall width={width} setWidth={setWidth} />;
};

export default VideoCallWrapper; 